<!doctype html>
<html lang="en">
 {{> head}}
  <body>
  
  {{> navbar}}
  
<div class = "container">
  
  <div class = "list-group">
    <ul>
    {{#insurance}}
    <div>
      <img src="/uploads/{{front_pic}}" class="labresult-preview-image" />
      <img src="/uploads/{{back_pic}}" class="labresult-preview-image" />
    </div>
    <div class = "d-flex w-100 justify-content-between">
      <h4 class="mb-1">{{care_provider_name}}</h4>
    </div>
    
    <form action = "/labresults/delete-labresult" method="POST">
      <input type="hidden" name= "front_pic" value="{{front_pic}}" />
      <input type="hidden" name="insurance_id" value="{{id}}" />
      <button type="submit" class = "btn btn-danger">Delete</button>
    </form>
  
   <a href="/labresults/edit/{{id}}"><button class="btn btn-info">Edit</button></a>
  
   {{/insurance}}

    </ul>
      
      {{#message}}
      <div class="alert alert-danger" role="alert">
      {{message}}
      </div>
      {{/message}}
    </div>
  </div>
  {{> footer}}
  </body>
</html>